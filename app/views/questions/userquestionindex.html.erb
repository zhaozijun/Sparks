<h1>Listing questions</h1>

<table>
  <tr>
		<th>No.</th>
		<th>Tag</th>
    <th>Content</th>
    <th>Anwsertext</th>
    <th>Paper</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @questions.each_with_index do |question, index| %>
  <tr>
    <td><%= index + 1 %></td>
		<td><%= question.tag %></td>
    <td><%= question.content %></td>
    <td><%= question.anwsertext %></td>
    <td><%= link_to 'Show', question_userquestionshow_path(@paper, question) %></td>
<% end %>
</table>

<br />

<%= link_to 'Back to Paper List Page', root_path %>
